# SCF Chatbot Frontend

Frontend React cho chatbot Mặt trận Văn hóa Sinh viên, được xây dựng với Vite + React + Tailwind CSS.

## 🚀 Cài đặt

```bash
npm install
cp .env.example .env
# Chỉnh sửa .env nếu backend chạy ở port khác
npm run dev
